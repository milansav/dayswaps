import styled from "@emotion/styled";

export const DayswapsButton = styled.button`
  background: #0045ff;
  transition: background 0.2s ease, color 0.2s ease, border 0.2s ease;
  font-weight: 700;
  color: #fff;
  width: 7.5rem;
  font-size: 0.75rem;
  height: 2rem;
  white-space: nowrap;
  border: none;
  border-radius: 0.5rem;
  cursor: pointer;

  &:hover {
    background: #2a4284;
  }
`;
